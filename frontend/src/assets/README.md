The ECO Energy Group logo is the 479-byte SVG supplied by the user (logo.svg). It is bundled locally, with no external references or runtime network dependencies.

Company brand blue: #2771f6. Theme colors follow report-hub frontend/app/globals.css at commit c4d694db03b5716a757f43337801999b6938f4f9. The default SVG is rendered white in dark mode via CSS; uploaded custom logos keep their original colors.
Custom kit logos and favicons override these defaults through System administration.

PFlow logo: user-supplied pflow.svg, with its fill changed to black; CSS renders it white in dark mode. Original paths are preserved.
