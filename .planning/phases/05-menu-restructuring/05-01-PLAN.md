---
phase: 05-menu-restructuring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/views/Interfaces.vue
  - frontend/src/views/Network.vue
  - frontend/src/views/System.vue
  - frontend/src/views/Monitoring.vue
autonomous: true

must_haves:
  truths:
    - "Four parent container views exist with v-tabs navigation structure"
    - "Each parent view dynamically generates tabs from child routes"
    - "Tab state syncs with route changes via watcher"
  artifacts:
    - path: "frontend/src/views/Interfaces.vue"
      provides: "Parent container for Modem + Serial tabs"
      min_lines: 40
    - path: "frontend/src/views/Network.vue"
      provides: "Parent container for Failover + VPN + WiFi tabs"
      min_lines: 40
    - path: "frontend/src/views/System.vue"
      provides: "Parent container for Settings + NTP + Backup tabs"
      min_lines: 40
    - path: "frontend/src/views/Monitoring.vue"
      provides: "Parent container for Diagnostics + Audit + SMS Alerts tabs"
      min_lines: 40
  key_links:
    - from: "frontend/src/views/Interfaces.vue"
      to: "router.getRoutes()"
      via: "computed childRoutes"
      pattern: "router\\.getRoutes\\(\\)\\.find"
    - from: "frontend/src/views/Interfaces.vue"
      to: "route.path"
      via: "watcher for tab sync"
      pattern: "watch\\(\\(\\) => route\\.path"
---

<objective>
Create four parent container views with tab-based navigation structure for consolidated menu grouping.

Purpose: Establish the container components that will host tabbed child views for Interfaces, Network, System, and Monitoring menu items, reducing navigation from 15 to 8 items.

Output: Four new .vue files (Interfaces.vue, Network.vue, System.vue, Monitoring.vue) with v-tabs navigation, router-view outlet, and route-based tab state management.
</objective>

<execution_context>
@/Users/jiridockal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jiridockal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jiridockal/development/ECO-IOT-GW/.planning/PROJECT.md
@/Users/jiridockal/development/ECO-IOT-GW/.planning/ROADMAP.md
@/Users/jiridockal/development/ECO-IOT-GW/.planning/STATE.md
@/Users/jiridockal/development/ECO-IOT-GW/.planning/phases/05-menu-restructuring/05-RESEARCH.md
@/Users/jiridockal/development/ECO-IOT-GW/frontend/src/App.vue
</context>

<tasks>

<task type="auto">
  <name>Create Interfaces.vue parent container</name>
  <files>frontend/src/views/Interfaces.vue</files>
  <action>
Create new parent container view for Interfaces menu item (combines Modem + Serial).

Structure:
- v-container fluid wrapper with page title "Interfaces"
- v-card containing v-tabs for navigation
- v-tabs with bg-color="primary", show-arrows, v-model bound to currentTab ref
- Dynamically generate v-tab elements from childRoutes computed property
- Each tab has :value and :to set to full child path (e.g., "/interfaces/modem")
- Include tabIcon from route meta if available
- v-card-text with router-view for child component rendering

Script setup:
- Import ref, computed, watch from vue
- Import useRoute, useRouter from vue-router
- Compute childRoutes by finding 'Interfaces' route and extracting children array
- Create currentTab ref initialized to route.path
- Watch route.path changes and sync to currentTab to prevent tab/route desync

Follow the pattern from RESEARCH.md "Parent Container View Template" (lines 572-636).
Use the exact structure with computed childRoutes and watch() for route sync.
Do NOT hard-code tab arrays - generate dynamically from router configuration.
  </action>
  <verify>
File exists at frontend/src/views/Interfaces.vue with:
- v-tabs element with v-model and bg-color
- router-view element for child rendering
- computed childRoutes extracting router children
- watch() syncing route.path to currentTab
  </verify>
  <done>Interfaces.vue exists with tab navigation structure, dynamic route-based tab generation, and route/tab state synchronization</done>
</task>

<task type="auto">
  <name>Create Network.vue parent container</name>
  <files>frontend/src/views/Network.vue</files>
  <action>
Create new parent container view for Network menu item (combines Failover + VPN + WiFi).

Use identical structure to Interfaces.vue with these adjustments:
- Page title: "Network"
- Route name to find: 'Network' (not 'Interfaces')
- Tab paths will be /network/failover, /network/vpn, /network/wifi

Copy the working pattern from Interfaces.vue, replacing only the title and route name.
Maintain the same computed childRoutes, watch(), v-tabs structure.
  </action>
  <verify>
File exists at frontend/src/views/Network.vue with:
- Page title "Network"
- Computed property finding 'Network' route
- Same v-tabs + router-view structure as Interfaces
  </verify>
  <done>Network.vue exists with tab navigation structure matching Interfaces pattern</done>
</task>

<task type="auto">
  <name>Create System.vue and Monitoring.vue parent containers</name>
  <files>
frontend/src/views/System.vue
frontend/src/views/Monitoring.vue
  </files>
  <action>
Create two more parent container views following the same pattern.

System.vue:
- Page title: "System"
- Route name: 'System'
- Will contain tabs for Settings + NTP + Backup

Monitoring.vue:
- Page title: "Monitoring"
- Route name: 'Monitoring'
- Will contain tabs for Diagnostics + Audit + SMS Alerts

Both use identical structure to Interfaces.vue and Network.vue - only title and route name differ.
Copy the working pattern to ensure consistency.
  </action>
  <verify>
Both files exist with:
- Correct page titles (System, Monitoring)
- Correct route name lookups
- Same v-tabs + router-view structure
  </verify>
  <done>System.vue and Monitoring.vue exist with tab navigation structure matching established pattern</done>
</task>

</tasks>

<verification>
All four parent container views created:
- frontend/src/views/Interfaces.vue
- frontend/src/views/Network.vue
- frontend/src/views/System.vue
- frontend/src/views/Monitoring.vue

Each file contains:
- v-tabs with dynamic route-based tab generation
- router-view for child component rendering
- computed childRoutes from router configuration
- watch() syncing route.path to currentTab
- Consistent structure following RESEARCH.md pattern
</verification>

<success_criteria>
Parent container views are ready to receive child routes and provide tab-based navigation once router configuration is updated in next plan.
</success_criteria>

<output>
After completion, create `.planning/phases/05-menu-restructuring/05-01-SUMMARY.md`
</output>
