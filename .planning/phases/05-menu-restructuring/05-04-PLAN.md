---
phase: 05-menu-restructuring
plan: 04
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - frontend/src/App.vue
autonomous: false

must_haves:
  truths:
    - "Navigation drawer shows 8 menu items instead of 15"
    - "Menu items point to parent routes (/interfaces, /network, /system, /monitoring)"
    - "Clicking menu items navigates to parent route which redirects to first tab"
  artifacts:
    - path: "frontend/src/App.vue"
      provides: "Updated menuItems array with 8 consolidated items"
      contains: "title: 'Interfaces'"
  key_links:
    - from: "frontend/src/App.vue menuItems"
      to: "frontend/src/router/index.js parent routes"
      via: "path properties match parent route paths"
      pattern: "path: '/interfaces'"
---

<objective>
Update App.vue navigation menu from 15 items to 8 consolidated items pointing to new parent routes.

Purpose: Complete the menu restructuring by updating the visible navigation to reflect the new tab-based grouping structure.

Output: Updated menuItems array in App.vue with 8 items, human verification checkpoint to confirm navigation works correctly.
</objective>

<execution_context>
@/Users/jiridockal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jiridockal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jiridockal/development/ECO-IOT-GW/.planning/PROJECT.md
@/Users/jiridockal/development/ECO-IOT-GW/.planning/ROADMAP.md
@/Users/jiridockal/development/ECO-IOT-GW/.planning/STATE.md
@/Users/jiridockal/development/ECO-IOT-GW/.planning/phases/05-menu-restructuring/05-RESEARCH.md
@/Users/jiridockal/development/ECO-IOT-GW/frontend/src/App.vue
</context>

<tasks>

<task type="auto">
  <name>Update App.vue menuItems array</name>
  <files>frontend/src/App.vue</files>
  <action>
Replace the menuItems array (currently lines 74-90) with consolidated 8-item version.

Current structure has 15 items:
- Dashboard, Docker, ThingsBoard, Terminal (unchanged - keep as-is)
- VPN, Diagnostics, Modem, Serial, WiFi, System, NTP, Backup, Network, SMS Alerts, Audit Log (to be consolidated)

New structure with 8 items:
```javascript
const menuItems = [
  { title: 'Dashboard', icon: 'mdi-view-dashboard', path: '/dashboard' },
  { title: 'Docker', icon: 'mdi-docker', path: '/docker' },
  { title: 'ThingsBoard', icon: 'mdi-cloud-sync', path: '/thingsboard' },
  { title: 'Terminal', icon: 'mdi-console', path: '/terminal' },
  { title: 'Interfaces', icon: 'mdi-ethernet', path: '/interfaces' },
  { title: 'Network', icon: 'mdi-network', path: '/network' },
  { title: 'System', icon: 'mdi-cog-outline', path: '/system' },
  { title: 'Monitoring', icon: 'mdi-monitor-dashboard', path: '/monitoring' }
]
```

CRITICAL path updates:
- '/interfaces' - will redirect to /interfaces/modem (Modem + Serial tabs)
- '/network' - will redirect to /network/failover (Failover + VPN + WiFi tabs)
- '/system' - will redirect to /system/settings (Settings + NTP + Backup tabs)
- '/monitoring' - will redirect to /monitoring/diagnostics (Diagnostics + Audit + SMS tabs)

This prevents "Menu Item Path Mismatch" pitfall (RESEARCH.md line 324).
Menu must navigate to parent routes, not old child paths like /modem or /ntp.
Router redirect handles showing the appropriate default tab.

No other changes to App.vue - keep drawer, rail, logout, snackbar logic unchanged.
  </action>
  <verify>
App.vue menuItems array contains exactly 8 items with paths:
- /dashboard, /docker, /thingsboard, /terminal (unchanged)
- /interfaces, /network, /system, /monitoring (new parent routes)
No menu items with old paths like /modem, /vpn, /ntp, /audit remain.
  </verify>
  <done>App.vue menuItems updated to 8 consolidated items pointing to parent routes</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete menu restructuring from 15 items to 8 tab-based navigation groups.

Built:
- 4 parent container views with v-tabs navigation
- Router configuration with nested routes and redirects
- 11 adapted child components without container wrappers
- Updated App.vue menu with 8 items
  </what-built>
  <how-to-verify>
1. Start development server:
   ```bash
   cd /Users/jiridockal/development/ECO-IOT-GW/frontend
   npm run dev
   ```

2. Open browser to http://localhost:3000 and login

3. Verify navigation drawer shows 8 menu items (not 15):
   - Dashboard, Docker, ThingsBoard, Terminal (4 standalone)
   - Interfaces, Network, System, Monitoring (4 with tabs)

4. Test Interfaces menu item:
   - Click "Interfaces" in nav drawer
   - Expect to see "Interfaces" page with 2 tabs: Modem, Serial
   - Click Modem tab - verify modem configuration UI loads
   - Click Serial tab - verify serial configuration UI loads
   - Check URL changes: /interfaces/modem, /interfaces/serial

5. Test Network menu item:
   - Click "Network" in nav drawer
   - Expect to see "Network" page with 3 tabs: Failover, VPN, WiFi AP
   - Verify all tabs load their respective UIs
   - Check URLs: /network/failover, /network/vpn, /network/wifi

6. Test System menu item:
   - Click "System" in nav drawer
   - Expect to see "System" page with 3 tabs: Settings, NTP, Backup
   - Verify all tabs load their respective UIs
   - Check URLs: /system/settings, /system/ntp, /system/backup

7. Test Monitoring menu item:
   - Click "Monitoring" in nav drawer
   - Expect to see "Monitoring" page with 3 tabs: Diagnostics, Audit Log, SMS Alerts
   - Verify all tabs load their respective UIs
   - Check URLs: /monitoring/diagnostics, /monitoring/audit, /monitoring/sms-alerts

8. Test browser navigation:
   - Use browser back/forward buttons
   - Verify tab highlighting syncs with URL (no desync)
   - Direct URL access: visit /interfaces/serial directly
   - Expect Serial tab to be highlighted/active

9. Test functionality preservation:
   - Try interacting with a few views (e.g., NTP configuration, Backup)
   - Verify existing features still work (no broken API calls, state management intact)

Expected results:
- 8 menu items visible
- Tab navigation works smoothly
- No layout issues (no double containers, excessive padding)
- No duplicate titles (parent provides title, tabs provide context)
- Browser history and direct URLs work correctly
- All existing functionality preserved
  </how-to-verify>
  <resume-signal>Type "approved" if navigation works correctly, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Navigation menu updated and tested:
- App.vue contains 8 menuItems pointing to correct parent routes
- Manual testing confirms all 4 tabbed views render correctly
- Tab switching works without layout conflicts
- Browser navigation syncs with tab state
- Existing functionality preserved in all child components
</verification>

<success_criteria>
User sees simplified 8-item navigation menu with tab-based grouping, can navigate between all views, and all features continue to work as before restructuring.
</success_criteria>

<output>
After completion, create `.planning/phases/05-menu-restructuring/05-04-SUMMARY.md`
</output>
