---
phase: 05-menu-restructuring
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - frontend/src/views/ModemConfig.vue
  - frontend/src/views/SerialConfig.vue
  - frontend/src/views/NetworkStatus.vue
  - frontend/src/views/VpnConfig.vue
  - frontend/src/views/WifiConfig.vue
  - frontend/src/views/SystemSettings.vue
  - frontend/src/views/NtpConfig.vue
  - frontend/src/views/Backup.vue
  - frontend/src/views/Diagnostics.vue
  - frontend/src/views/AuditLog.vue
  - frontend/src/views/SmsAlerts.vue
autonomous: true

must_haves:
  truths:
    - "Child components no longer have v-container wrapper causing double nesting"
    - "Child components no longer have top-level h1 titles (parent provides context)"
    - "Child components retain all business logic and functionality"
  artifacts:
    - path: "frontend/src/views/ModemConfig.vue"
      provides: "Modem configuration without container wrapper"
      contains: "<v-row>"
  key_links:
    - from: "frontend/src/views/ModemConfig.vue template"
      to: "business logic in script"
      via: "no script changes, only template wrapper removal"
      pattern: "<script setup>"
---

<objective>
Adapt existing child view components to work within tabbed parent containers by removing outer wrappers that cause layout conflicts.

Purpose: Prevent double v-container nesting and duplicate titles when existing standalone views are rendered as tab children, while preserving all business logic.

Output: 11 updated child component files with v-container and title rows removed from templates, script sections unchanged.
</objective>

<execution_context>
@/Users/jiridockal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jiridockal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jiridockal/development/ECO-IOT-GW/.planning/PROJECT.md
@/Users/jiridockal/development/ECO-IOT-GW/.planning/ROADMAP.md
@/Users/jiridockal/development/ECO-IOT-GW/.planning/STATE.md
@/Users/jiridockal/development/ECO-IOT-GW/.planning/phases/05-menu-restructuring/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Adapt Interfaces child components (Modem, Serial)</name>
  <files>
frontend/src/views/ModemConfig.vue
frontend/src/views/SerialConfig.vue
  </files>
  <action>
Remove outer container wrappers from ModemConfig.vue and SerialConfig.vue to prevent layout conflicts when embedded in Interfaces parent tabs.

For each file:
1. Remove opening `<v-container fluid>` tag from template
2. Remove closing `</v-container>` tag from template
3. Remove the title row if it contains an h1/h2 with component name (e.g., `<h1 class="text-h4 mb-4">Modem Configuration</h1>`)
4. Keep all remaining v-row, v-col, v-card content
5. DO NOT modify script setup section - preserve all imports, refs, functions, lifecycle hooks
6. DO NOT modify style section if present

Parent view (Interfaces.vue) now provides:
- Container context via v-container fluid
- Page title "Interfaces"
- Tab label shows which interface (Modem/Serial)

Result: Template starts directly with content `<v-row>` elements, no wrapper container.

Follow pattern from RESEARCH.md "Adapted Child Component Example" (lines 639-690).
This prevents "Container/Layout Conflicts" pitfall (RESEARCH.md line 289).
  </action>
  <verify>
Both files no longer have:
- `<v-container fluid>` as root template element
- Top-level title row with h1
But still have:
- All v-row/v-col/v-card content structures
- Unchanged script setup section with all business logic
  </verify>
  <done>ModemConfig.vue and SerialConfig.vue adapted for tab embedding without container/title conflicts</done>
</task>

<task type="auto">
  <name>Adapt Network child components (NetworkStatus, VPN, WiFi)</name>
  <files>
frontend/src/views/NetworkStatus.vue
frontend/src/views/VpnConfig.vue
frontend/src/views/WifiConfig.vue
  </files>
  <action>
Remove outer container wrappers from Network child components.

For each file (NetworkStatus.vue, VpnConfig.vue, WifiConfig.vue):
1. Remove `<v-container fluid>` wrapper tags
2. Remove title row with h1 if present
3. Keep all v-row/v-col/v-card content
4. Preserve script setup section completely unchanged

NetworkStatus.vue becomes the "Failover" tab within Network parent.
Parent provides "Network" title, tab shows "Failover" label.

Same pattern as Interfaces components - remove wrapper, keep content and logic.
  </action>
  <verify>
All three Network child components:
- No v-container wrapper
- No top-level h1 title
- Script sections unchanged
- Content structures preserved
  </verify>
  <done>NetworkStatus.vue, VpnConfig.vue, WifiConfig.vue adapted for tab embedding</done>
</task>

<task type="auto">
  <name>Adapt System and Monitoring child components</name>
  <files>
frontend/src/views/SystemSettings.vue
frontend/src/views/NtpConfig.vue
frontend/src/views/Backup.vue
frontend/src/views/Diagnostics.vue
frontend/src/views/AuditLog.vue
frontend/src/views/SmsAlerts.vue
  </files>
  <action>
Remove outer container wrappers from System and Monitoring child components.

System children (SystemSettings.vue, NtpConfig.vue, Backup.vue):
- Remove v-container wrapper
- Remove title rows
- Keep content and business logic intact
- Parent System.vue provides "System" title, tabs show "Settings"/"NTP"/"Backup"

Monitoring children (Diagnostics.vue, AuditLog.vue, SmsAlerts.vue):
- Remove v-container wrapper
- Remove title rows
- Keep content and business logic intact
- Parent Monitoring.vue provides "Monitoring" title, tabs show "Diagnostics"/"Audit Log"/"SMS Alerts"

Apply same pattern consistently across all 6 files.
Script sections remain completely unchanged in all files.
  </action>
  <verify>
All six components:
- Templates start with content (v-row or other), not v-container
- No top-level page titles
- Script setup sections unchanged
- All API calls, state management, composables still present
  </verify>
  <done>All System and Monitoring child components adapted for tab embedding, completing adaptation of 11 child components total</done>
</task>

</tasks>

<verification>
All 11 child components adapted:
- Interfaces: ModemConfig.vue, SerialConfig.vue
- Network: NetworkStatus.vue, VpnConfig.vue, WifiConfig.vue
- System: SystemSettings.vue, NtpConfig.vue, Backup.vue
- Monitoring: Diagnostics.vue, AuditLog.vue, SmsAlerts.vue

Changes applied to each:
- v-container wrapper removed from template
- Top-level title row removed where present
- Content structure (v-row/v-col/v-card) preserved
- Script sections completely unchanged
- No business logic modified
</verification>

<success_criteria>
Child components render correctly within parent tab containers without double nesting or duplicate titles, all functionality preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/05-menu-restructuring/05-03-SUMMARY.md`
</output>
