---
phase: 04-sms-alerts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/schemas.py
  - backend/app/services/sms_service.py
  - backend/app/services/modem_service.py
  - backend/requirements.txt
autonomous: true

must_haves:
  truths:
    - "SMS recipients can be stored and retrieved"
    - "Phone numbers are validated to E.164 format"
    - "SMS can be sent via AT commands on modem"
    - "Serial port access is thread-safe"
  artifacts:
    - path: "backend/app/models/schemas.py"
      provides: "SMSRecipient, SMSConfig, SMSTrigger, SMSTestRequest, SMSTestResponse models"
      contains: "class SMSConfig"
    - path: "backend/app/services/sms_service.py"
      provides: "SMS configuration management with encrypted storage"
      exports: ["sms_service"]
    - path: "backend/app/services/modem_service.py"
      provides: "send_sms method with thread-safe serial access"
      contains: "_serial_lock"
  key_links:
    - from: "backend/app/services/sms_service.py"
      to: "backend/app/services/modem_service.py"
      via: "modem_service.send_sms() call"
      pattern: "modem_service\\.send_sms"
    - from: "backend/app/services/sms_service.py"
      to: "/var/lib/eco-iot-gw/sms_config.json"
      via: "encrypted JSON storage"
      pattern: "sms_config\\.json"
---

<objective>
Create backend SMS foundation: Pydantic models, SMS service for configuration, and extend modem service with SMS sending capability.

Purpose: Establish the core SMS infrastructure following project patterns (encrypted config, audit logging, AT commands).
Output: Working SMS send functionality and configuration management ready for API layer.
</objective>

<execution_context>
@/Users/jiridockal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jiridockal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-sms-alerts/04-RESEARCH.md

# Existing patterns to follow
@backend/app/services/modem_service.py
@backend/app/models/schemas.py
@backend/app/services/ntp_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SMS Pydantic Models</name>
  <files>backend/app/models/schemas.py</files>
  <action>
Add SMS-related Pydantic models to schemas.py following existing patterns:

1. **SMSRecipient** model:
   - `name: str` (Field min_length=1, max_length=50)
   - `phone: str` (Field min_length=1, max_length=20) - stored as E.164
   - `enabled: bool = True`

2. **SMSTrigger** model:
   - `event_type: str` (Field pattern for valid events: vpn_down, modem_down, network_failover, backup_failed)
   - `enabled: bool = True`
   - `cooldown_minutes: int` (Field default=30, ge=0, le=1440)

3. **SMSConfig** model:
   - `enabled: bool = False`
   - `recipients: List[SMSRecipient] = []`
   - `triggers: List[SMSTrigger] = []`
   - `default_region: str` (Field default="CZ", pattern for ISO country code)

4. **SMSTestRequest** model:
   - `phone: str` (Field min_length=1, max_length=20)
   - `message: Optional[str] = None` (optional custom message)

5. **SMSTestResponse** model:
   - `success: bool`
   - `message: str`
   - `message_reference: Optional[str] = None`

Add these in a new "SMS Models" section after Network Models.
  </action>
  <verify>
Run `python -c "from backend.app.models.schemas import SMSConfig, SMSRecipient, SMSTrigger, SMSTestRequest, SMSTestResponse; print('OK')"` from project root.
  </verify>
  <done>
All five SMS models importable and have correct field definitions with validation.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create SMS Service</name>
  <files>backend/app/services/sms_service.py</files>
  <action>
Create sms_service.py following ntp_service.py and modem_service.py patterns:

1. **Imports**: json, logging, threading, Path, Optional, List, phonenumbers from phonenumbers library
2. **Import from project**: settings, SMSConfig/SMSRecipient/SMSTrigger from schemas, encrypt/decrypt from crypto, audit_service

3. **validate_phone_number() function**:
   - Takes phone: str, default_region: str = "CZ"
   - Uses phonenumbers.parse() and is_valid_number()
   - Returns E.164 formatted string
   - Raises ValueError for invalid numbers

4. **SMSService class**:
   - `__init__`: Set config_file path to settings.DATA_DIR / "sms_config.json", call _load_config()
   - `_load_config()`: Load JSON, decrypt phone numbers using decrypt_sensitive_data, parse into SMSConfig
   - `_save_config()`: Encrypt phone numbers, write JSON with 0o600 permissions
   - `get_config() -> SMSConfig`: Return current config or default
   - `set_config(config: SMSConfig, username: str, ip_address: str)`: Validate all phone numbers to E.164, save, audit log
   - `add_recipient(recipient: SMSRecipient, username: str, ip_address: str)`: Validate phone, add to list, save, audit log
   - `remove_recipient(name: str, username: str, ip_address: str)`: Remove by name, save, audit log
   - `get_default_region() -> str`: Return config's default_region

5. **Global instance**: `sms_service = SMSService()`

6. **Add phonenumbers to requirements.txt**: `phonenumbers==9.0.22`

Follow the config encryption pattern from modem_service.py exactly.
  </action>
  <verify>
1. Check requirements.txt has phonenumbers: `grep phonenumbers backend/requirements.txt`
2. Test import: `cd backend && python -c "from app.services.sms_service import sms_service, validate_phone_number; print(validate_phone_number('+420777123456', 'CZ'))"`
  </verify>
  <done>
sms_service instantiates without error, validate_phone_number returns E.164 format, config load/save works.
  </done>
</task>

<task type="auto">
  <name>Task 3: Extend Modem Service with SMS</name>
  <files>backend/app/services/modem_service.py</files>
  <action>
Extend ModemService class with thread-safe SMS sending:

1. **Add import**: `import threading` at top

2. **Add lock in __init__**:
   ```python
   self._serial_lock = threading.Lock()
   ```

3. **Add check_sms_ready() method**:
   - Send AT+CPIN? to verify SIM ready
   - Return tuple (ready: bool, message: str)

4. **Add send_sms(phone_number: str, message: str) method**:
   - Wrap ALL serial operations in `with self._serial_lock:`
   - Find modem port
   - Check SIM ready (AT+CPIN?)
   - Set text mode (AT+CMGF=1)
   - Set GSM charset (AT+CSCS="GSM")
   - Send AT+CMGS="{phone_number}"
   - Wait for '>' prompt
   - Send message text + Ctrl-Z (0x1A)
   - Wait for +CMGS: response with 30s timeout
   - Return tuple (success: bool, message_or_error: str)
   - Handle all exceptions, return (False, error_msg)

5. **Update send_at_command() to use lock**:
   - Wrap serial operations in `with self._serial_lock:`

Follow the complete SMS send sequence from RESEARCH.md (30s timeout, error handling, message reference extraction).

Truncate messages to 160 chars (GSM-7 safe length).
  </action>
  <verify>
Test import and instantiation: `cd backend && python -c "from app.services.modem_service import modem_service; print('Lock:', hasattr(modem_service, '_serial_lock')); print('SMS:', hasattr(modem_service, 'send_sms'))"`
  </verify>
  <done>
ModemService has _serial_lock, send_sms method, and check_sms_ready method. Existing send_at_command still works.
  </done>
</task>

</tasks>

<verification>
1. All SMS models importable from schemas.py
2. sms_service loads/saves config with encryption
3. validate_phone_number accepts various formats and returns E.164
4. modem_service.send_sms exists and uses serial lock
5. `pip install phonenumbers==9.0.22` succeeds
</verification>

<success_criteria>
- SMSConfig, SMSRecipient, SMSTrigger models defined with validation
- sms_service handles encrypted config storage at /var/lib/eco-iot-gw/sms_config.json
- Phone number validation normalizes to E.164 format
- modem_service.send_sms() implements complete AT command sequence with thread safety
- No import errors, all services instantiate
</success_criteria>

<output>
After completion, create `.planning/phases/04-sms-alerts/04-01-SUMMARY.md`
</output>
