---
phase: 04-sms-alerts
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - frontend/src/views/SmsAlerts.vue
  - frontend/src/services/api.js
  - frontend/src/router/index.js
autonomous: false

must_haves:
  truths:
    - "User can view SMS configuration"
    - "Admin can add/remove SMS recipients"
    - "User can test SMS functionality"
    - "Phone numbers are validated on input"
    - "UI shows success/error feedback"
  artifacts:
    - path: "frontend/src/views/SmsAlerts.vue"
      provides: "SMS configuration and testing UI"
      min_lines: 200
    - path: "frontend/src/services/api.js"
      provides: "smsApi object for API calls"
      contains: "smsApi"
    - path: "frontend/src/router/index.js"
      provides: "Route to SMS view"
      contains: "/sms"
  key_links:
    - from: "frontend/src/views/SmsAlerts.vue"
      to: "frontend/src/services/api.js"
      via: "smsApi calls"
      pattern: "smsApi\\.(getConfig|setConfig|test)"
    - from: "frontend/src/router/index.js"
      to: "frontend/src/views/SmsAlerts.vue"
      via: "route component"
      pattern: "SmsAlerts"
---

<objective>
Create frontend SMS Alerts view with recipient management and test functionality.

Purpose: Enable users to configure SMS recipients and test SMS sending through the web UI.
Output: Working SmsAlerts.vue component accessible from navigation.
</objective>

<execution_context>
@/Users/jiridockal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jiridockal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-sms-alerts/04-01-SUMMARY.md
@.planning/phases/04-sms-alerts/04-02-SUMMARY.md

# Existing patterns to follow
@frontend/src/views/NetworkStatus.vue
@frontend/src/views/NtpConfig.vue
@frontend/src/services/api.js
@frontend/src/router/index.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add smsApi to api.js</name>
  <files>frontend/src/services/api.js</files>
  <action>
Add smsApi object following ntpApi and networkApi patterns:

```javascript
export const smsApi = {
  getConfig: () => api.get('/api/sms/config'),
  setConfig: (config) => api.put('/api/sms/config', config),
  addRecipient: (recipient) => api.post('/api/sms/recipients', recipient),
  removeRecipient: (name) => api.delete(`/api/sms/recipients/${encodeURIComponent(name)}`),
  validateNumber: (phone, region = 'CZ') => api.post('/api/sms/validate-number', null, {
    params: { phone, region }
  }),
  testSms: (phone, message = null) => api.post('/api/sms/test', { phone, message }),
  getTriggers: () => api.get('/api/sms/triggers')
}
```

Add this after the existing API objects (networkApi, backupApi, etc.).
  </action>
  <verify>
Check syntax: `cd frontend && node -e "const fs = require('fs'); const content = fs.readFileSync('src/services/api.js', 'utf8'); if (content.includes('smsApi')) console.log('smsApi found'); else process.exit(1)"`
  </verify>
  <done>
smsApi object exported from api.js with all required methods.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create SmsAlerts.vue Component</name>
  <files>frontend/src/views/SmsAlerts.vue</files>
  <action>
Create SmsAlerts.vue following NetworkStatus.vue and NtpConfig.vue patterns:

**Template structure (3 cards):**

1. **SMS Configuration Card**:
   - v-switch for enabled/disabled
   - v-select for default_region (common country codes: CZ, DE, AT, SK, PL, US, GB)
   - Save button (admin only, disabled for non-admin)

2. **Recipients Card**:
   - v-data-table showing recipients (name, phone, enabled status)
   - Add recipient dialog with:
     - v-text-field for name
     - v-text-field for phone with validation
     - v-switch for enabled
   - Delete button per row (admin only)
   - Phone validation on blur (call validateNumber API)
   - Show validation result (valid/invalid, E.164 format)

3. **Test SMS Card**:
   - v-text-field for phone number (with validation)
   - v-textarea for optional custom message (max 160 chars, show counter)
   - Send Test button
   - Result display (success/error with message reference)

**Script setup:**
- Import smsApi from api.js
- Import useAuthStore from auth.js for role check
- Reactive state: config, recipients, loading, error, testPhone, testMessage, testResult
- onMounted: load config
- Methods: loadConfig, saveConfig, addRecipient, removeRecipient, validatePhone, sendTest

**UI patterns to follow:**
- Use v-card with title and subtitle
- Use v-alert for errors
- Use v-snackbar for success messages
- Admin-only controls: `:disabled="!isAdmin"` with "(Admin only)" hint
- Loading states on buttons

**Vuetify components:**
- v-container, v-row, v-col for layout
- v-card, v-card-title, v-card-text, v-card-actions
- v-switch, v-select, v-text-field, v-textarea
- v-btn with :loading prop
- v-data-table for recipients
- v-dialog for add recipient
- v-chip for phone validation status
  </action>
  <verify>
Check syntax: `cd frontend && npm run lint -- --max-warnings=5 src/views/SmsAlerts.vue 2>&1 | tail -5`
  </verify>
  <done>
SmsAlerts.vue component created with configuration, recipients table, and test functionality.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add Route and Navigation</name>
  <files>frontend/src/router/index.js</files>
  <action>
Add SMS Alerts route to router:

1. Import SmsAlerts component (lazy load):
   ```javascript
   const SmsAlerts = () => import('../views/SmsAlerts.vue')
   ```

2. Add route in routes array (after network-status):
   ```javascript
   {
     path: '/sms-alerts',
     name: 'SmsAlerts',
     component: SmsAlerts,
     meta: { requiresAuth: true }
   }
   ```

3. Check if navigation is in a separate file or in App.vue. Add navigation item:
   - Icon: mdi-message-text or mdi-cellphone-message
   - Title: "SMS Alerts"
   - To: "/sms-alerts"

Place after "Network Status" in navigation order.
  </action>
  <verify>
Check route: `cd frontend && node -e "const fs = require('fs'); const content = fs.readFileSync('src/router/index.js', 'utf8'); if (content.includes('sms-alerts')) console.log('Route found'); else process.exit(1)"`
  </verify>
  <done>
Route /sms-alerts configured, navigation item added, requires authentication.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify SMS Alerts UI</name>
  <what-built>Complete SMS Alerts feature: backend API + frontend UI for configuring recipients and testing SMS</what-built>
  <how-to-verify>
1. Start backend: `cd backend && source venv/bin/activate && uvicorn app.main:app --reload`
2. Start frontend: `cd frontend && npm run dev`
3. Open http://localhost:3000 and login
4. Navigate to "SMS Alerts" in sidebar
5. Verify UI elements:
   - Configuration card shows enabled toggle and region selector
   - Recipients table loads (may be empty)
   - Test SMS section has phone input and send button
6. Try adding a recipient (admin only)
7. Try phone validation (enter number, tab out) - verify E.164 format shown
8. **REQUIRED**: Send test SMS to verify modem communication:
   - Enter a valid phone number
   - Click "Send Test"
   - Verify success response with message reference OR
   - If modem unavailable, verify error message is displayed correctly
  </how-to-verify>
  <resume-signal>Type "approved" after verifying SMS test works (or displays proper error)</resume-signal>
</task>

</tasks>

<verification>
1. Frontend builds without errors: `npm run build`
2. /sms-alerts route accessible after login
3. smsApi methods work (network tab shows API calls)
4. Admin-only controls are properly disabled for non-admin users
5. Phone validation provides feedback
</verification>

<success_criteria>
- SmsAlerts.vue displays SMS configuration
- Recipients can be added/removed (admin only)
- Phone numbers validated with visual feedback
- Test SMS can be sent with result display
- Navigation accessible from sidebar
</success_criteria>

<output>
After completion, create `.planning/phases/04-sms-alerts/04-03-SUMMARY.md`
</output>
